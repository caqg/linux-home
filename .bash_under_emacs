# -*- Shell-Script -*- vim: ft=sh :

if [ "$WSL_DISTRO_NAME" ]; then
	export EDITOR=emacsclient
	export VISUAL=emacsclient
else
	export EDITOR=$HOME/cmd/e
	export VISUAL=$HOME/cmd/e
fi

export PAGER=/bin/cat
export MANPAGER=

export TERM=ansi
uncolorize-ls

##end .bash_under_emacs
