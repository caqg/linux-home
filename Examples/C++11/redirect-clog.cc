#include <algorithm>
#include <functional>
#include <iostream>
#include <iterator>
#include <string>
using namespace std;

int main(int argc, char** argv)
{
        // Each argument names an output file, which is accessed thru std::clog
        for (int i = 1; i < argc; ++i) {
                
        }
}

// Local Variables:
// compile-command: "time -p LANG=C c++ -g -O3 -std=c++11 redirect-clog.cc -o redirect-clog"
// End:
